#include "main.h"
/**
 * main - tests function that prints if integer is positive or negative
 *
 * Description: this test the positive_or_negative function by providing
 * an input value to test with.
 * Return: 0 on success.
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);
	return (0);
}
